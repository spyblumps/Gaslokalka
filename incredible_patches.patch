From 5d8bbdde45c2005c55f8b48879d33df1466a1fbc Mon Sep 17 00:00:00 2001
From: loltart <159829224+loltart@users.noreply.github.com>
Date: Thu, 13 Aug 2026 18:51:49 -0700
Subject: [PATCH] hallucinations

---
 .../HallucinationAppearanceSystem.cs          | 175 ++++
 .../Chat/BeforeChatMessageSentEvent.cs        |  11 +
 .../Chat/ChatOnlyHeardByComponent.cs          |  22 +
 .../Chat/ChatOnlyHeardBySystem.cs             |  87 ++
 .../Hallucinations/HallucinationSystem.cs     | 351 ++++++++
 .../Hallucinations/HallucinatingComponent.cs  |  40 +
 .../Hallucinations/HallucinationEffects.cs    | 135 ++++
 .../Hallucinations/HallucinationPrototype.cs  |  19 +
 .../PhantomHallucinationComponent.cs          |  31 +
 Content.Server/Chat/Systems/ChatSystem.cs     |   7 +
 Content.Server/Ghost/GhostSystem.cs           |   1 +
 Content.Shared/Eye/VisibilityFlags.cs         |   1 +
 .../hallucinations/fake-conversations.ftl     |  60 ++
 Resources/Prototypes/Reagents/toxins.yml      |   5 +-
 .../Hallucinations/hallucinations.yml         | 752 ++++++++++++++++++
 15 files changed, 1696 insertions(+), 1 deletion(-)
 create mode 100644 Content.Goobstation.Client/Hallucinations/HallucinationAppearanceSystem.cs
 create mode 100644 Content.Goobstation.Common/Chat/BeforeChatMessageSentEvent.cs
 create mode 100644 Content.Goobstation.Server/Chat/ChatOnlyHeardByComponent.cs
 create mode 100644 Content.Goobstation.Server/Chat/ChatOnlyHeardBySystem.cs
 create mode 100644 Content.Goobstation.Server/Hallucinations/HallucinationSystem.cs
 create mode 100644 Content.Goobstation.Shared/Hallucinations/HallucinatingComponent.cs
 create mode 100644 Content.Goobstation.Shared/Hallucinations/HallucinationEffects.cs
 create mode 100644 Content.Goobstation.Shared/Hallucinations/HallucinationPrototype.cs
 create mode 100644 Content.Goobstation.Shared/Hallucinations/PhantomHallucinationComponent.cs
 create mode 100644 Resources/Locale/en-US/_Goobstation/hallucinations/fake-conversations.ftl
 create mode 100644 Resources/Prototypes/_Goobstation/Hallucinations/hallucinations.yml

diff --git a/Content.Goobstation.Client/Hallucinations/HallucinationAppearanceSystem.cs b/Content.Goobstation.Client/Hallucinations/HallucinationAppearanceSystem.cs
new file mode 100644
index 000000000000..b77904647d7d
--- /dev/null
+++ b/Content.Goobstation.Client/Hallucinations/HallucinationAppearanceSystem.cs
@@ -0,0 +1,175 @@
+using Content.Goobstation.Shared.Hallucinations;
+using Content.Shared.Humanoid;
+using Robust.Client.GameObjects;
+using Robust.Client.Player;
+using Robust.Shared.Audio.Systems;
+using Robust.Shared.Prototypes;
+using Robust.Shared.Random;
+using Robust.Shared.Timing;
+
+namespace Content.Goobstation.Client.Hallucinations;
+
+/// <summary>
+/// Client half of the hallucinations.
+/// </summary>
+public sealed class HallucinationAppearanceSystem : EntitySystem
+{
+    [Dependency] private readonly SharedAudioSystem _audio = default!;
+    [Dependency] private readonly IPlayerManager _player = default!;
+    [Dependency] private readonly IPrototypeManager _proto = default!;
+    [Dependency] private readonly IRobustRandom _random = default!;
+    [Dependency] private readonly SpriteSystem _sprite = default!;
+    [Dependency] private readonly IGameTiming _timing = default!;
+    [Dependency] private readonly SharedTransformSystem _transform = default!;
+
+    private const string LayerKey = "hallucination-appearance";
+    private const string MorphPrefix = "hallucination-morph-";
+    private const float SearchRange = 10f;
+    private static readonly TimeSpan Lifetime = TimeSpan.FromSeconds(15);
+
+    private readonly Dictionary<EntityUid, Fake> _fakes = new();
+
+    public override void Initialize()
+    {
+        base.Initialize();
+
+        SubscribeNetworkEvent<SetHallucinationAppearanceMessage>(OnAppearanceMessage);
+        SubscribeNetworkEvent<SetHallucinationMorphMessage>(OnMorphMessage);
+    }
+
+    private void OnAppearanceMessage(SetHallucinationAppearanceMessage args)
+    {
+        if (_player.LocalEntity is not { } localPlayer
+            || args.Prototypes.Count == 0
+            || args.States.Count == 0
+            || !TryPickTarget(localPlayer, out var target))
+            return;
+
+        if (!_proto.TryIndex<EntityPrototype>(_random.Pick(args.Prototypes), out var proto)
+            || !proto.TryGetComponent<SpriteComponent>(out var itemSprite, EntityManager.ComponentFactory)
+            || itemSprite.BaseRSI is not { } rsi)
+            return;
+
+        var state = _random.Pick(args.States);
+        if (!rsi.TryGetState(state, out _))
+            return;
+
+        _sprite.LayerMapReserve(target, LayerKey);
+        _sprite.LayerSetData(target, LayerKey, new PrototypeLayerData { RsiPath = rsi.Path.ToString(), State = state });
+        _fakes[target] = new Fake { End = _timing.CurTime + Lifetime, Added = { LayerKey } };
+
+        if (_player.LocalSession != null)
+            _audio.PlayEntity(args.Sound, _player.LocalSession, target);
+    }
+
+    private void OnMorphMessage(SetHallucinationMorphMessage args)
+    {
+        if (_player.LocalEntity is not { } localPlayer
+            || args.Mobs.Count == 0
+            || !_proto.TryIndex<EntityPrototype>(_random.Pick(args.Mobs), out var proto)
+            || !proto.TryGetComponent<SpriteComponent>(out var mobSprite, EntityManager.ComponentFactory)
+            || !TryPickTarget(localPlayer, out var target)
+            || !TryComp<SpriteComponent>(target, out var sprite))
+            return;
+
+        var hidden = new List<int>();
+        var index = 0;
+        foreach (var layer in sprite.AllLayers)
+        {
+            if (layer.Visible)
+                hidden.Add(index);
+            index++;
+        }
+
+        var fake = new Fake { End = _timing.CurTime + Lifetime, Hidden = hidden };
+        var next = 0;
+        foreach (var layer in mobSprite.AllLayers)
+        {
+            if (layer.ActualRsi is not { } rsi || layer.RsiState.Name is not { } state)
+                continue;
+
+            var key = MorphPrefix + next++;
+            _sprite.LayerMapReserve((target, sprite), key);
+            _sprite.LayerSetData((target, sprite), key, new PrototypeLayerData { RsiPath = rsi.Path.ToString(), State = state });
+            fake.Added.Add(key);
+        }
+
+        if (fake.Added.Count == 0)
+            return;
+
+        foreach (var i in hidden)
+            _sprite.LayerSetVisible((target, sprite), i, false);
+
+        _fakes[target] = fake;
+    }
+
+    private bool TryPickTarget(EntityUid localPlayer, out EntityUid target)
+    {
+        target = default;
+
+        var playerCoords = _transform.GetMapCoordinates(localPlayer);
+        var candidates = new List<EntityUid>();
+
+        var query = EntityQueryEnumerator<HumanoidAppearanceComponent, SpriteComponent, TransformComponent>();
+        while (query.MoveNext(out var uid, out _, out _, out var xform))
+        {
+            if (uid == localPlayer || _fakes.ContainsKey(uid))
+                continue;
+
+            var coords = _transform.GetMapCoordinates(uid, xform);
+            if (coords.MapId != playerCoords.MapId
+                || (coords.Position - playerCoords.Position).LengthSquared() > SearchRange * SearchRange)
+                continue;
+
+            candidates.Add(uid);
+        }
+
+        if (candidates.Count == 0)
+            return false;
+
+        target = _random.Pick(candidates);
+        return true;
+    }
+
+    public override void FrameUpdate(float frameTime)
+    {
+        base.FrameUpdate(frameTime);
+
+        if (_fakes.Count == 0)
+            return;
+
+        var expired = new List<EntityUid>();
+        foreach (var (uid, fake) in _fakes)
+            if (fake.End <= _timing.CurTime)
+                expired.Add(uid);
+
+        foreach (var uid in expired)
+        {
+            var fake = _fakes[uid];
+            _fakes.Remove(uid);
+            Restore(uid, fake);
+        }
+    }
+
+    private void Restore(EntityUid uid, Fake fake)
+    {
+        if (TerminatingOrDeleted(uid) || !TryComp<SpriteComponent>(uid, out var sprite))
+            return;
+
+        foreach (var key in fake.Added)
+            _sprite.RemoveLayer((uid, sprite), key, false);
+
+        foreach (var index in fake.Hidden)
+            _sprite.LayerSetVisible((uid, sprite), index, true);
+    }
+
+    /// <summary>
+    /// Layers added (to remove) and hidden (to show again).
+    /// </summary>
+    private sealed class Fake
+    {
+        public TimeSpan End;
+        public List<string> Added = new();
+        public List<int> Hidden = new();
+    }
+}
diff --git a/Content.Goobstation.Common/Chat/BeforeChatMessageSentEvent.cs b/Content.Goobstation.Common/Chat/BeforeChatMessageSentEvent.cs
new file mode 100644
index 000000000000..20be903f964e
--- /dev/null
+++ b/Content.Goobstation.Common/Chat/BeforeChatMessageSentEvent.cs
@@ -0,0 +1,11 @@
+namespace Content.Goobstation.Common.Chat;
+
+/// <summary>
+/// Raised at an entity after its IC chat message is sanitized but before it is sent, letting
+/// systems reroute or suppress it entirely (e.g. hallucinations only their victim can hear).
+/// </summary>
+[ByRefEvent]
+public record struct BeforeChatMessageSentEvent(string Message, byte Channel)
+{
+    public bool Cancelled;
+}
diff --git a/Content.Goobstation.Server/Chat/ChatOnlyHeardByComponent.cs b/Content.Goobstation.Server/Chat/ChatOnlyHeardByComponent.cs
new file mode 100644
index 000000000000..5bc72709bb63
--- /dev/null
+++ b/Content.Goobstation.Server/Chat/ChatOnlyHeardByComponent.cs
@@ -0,0 +1,22 @@
+namespace Content.Goobstation.Server.Chat;
+
+/// <summary>
+/// Suppresses this entity's IC chat (speech, whispers and emotes) for everyone except
+/// the entity itself and the listener, who still needs to be in normal hearing range.
+/// </summary>
+[RegisterComponent]
+public sealed partial class ChatOnlyHeardByComponent : Component
+{
+    /// <summary>
+    /// The only entity besides the speaker that receives the messages.
+    /// If null the speaker just talks to themself.
+    /// </summary>
+    [DataField]
+    public EntityUid? Listener;
+
+    /// <summary>
+    /// Optional color applied to the speaker's name in spoken and whispered messages.
+    /// </summary>
+    [DataField]
+    public Color? NameColor;
+}
diff --git a/Content.Goobstation.Server/Chat/ChatOnlyHeardBySystem.cs b/Content.Goobstation.Server/Chat/ChatOnlyHeardBySystem.cs
new file mode 100644
index 000000000000..ed995193aa22
--- /dev/null
+++ b/Content.Goobstation.Server/Chat/ChatOnlyHeardBySystem.cs
@@ -0,0 +1,87 @@
+using Content.Goobstation.Common.Chat;
+using Content.Server.Chat.Managers;
+using Content.Server.Chat.Systems;
+using Content.Shared.Chat;
+using Content.Shared.Ghost;
+using Robust.Server.Player;
+using Robust.Shared.Utility;
+
+namespace Content.Goobstation.Server.Chat;
+
+/// <summary>
+/// Reroutes the IC chat of entities with ChatOnlyHeardByComponent
+/// so only the speaker and their designated listener receive it.
+/// </summary>
+public sealed class ChatOnlyHeardBySystem : EntitySystem
+{
+    [Dependency] private readonly ChatSystem _chat = default!;
+    [Dependency] private readonly IChatManager _chatManager = default!;
+    [Dependency] private readonly IPlayerManager _player = default!;
+
+    public override void Initialize()
+    {
+        base.Initialize();
+
+        SubscribeLocalEvent<ChatOnlyHeardByComponent, BeforeChatMessageSentEvent>(OnBeforeChatSent);
+    }
+
+    private void OnBeforeChatSent(Entity<ChatOnlyHeardByComponent> ent, ref BeforeChatMessageSentEvent args)
+    {
+        args.Cancelled = true;
+
+        var channel = (InGameICChatType) args.Channel;
+        var name = ent.Comp.NameColor is { } color
+            ? $"[color={color.ToHex()}]{Name(ent)}[/color]"
+            : Name(ent);
+
+        var wrapped = channel switch
+        {
+            InGameICChatType.Speak => _chat.WrapPublicMessage(ent, name, args.Message),
+            InGameICChatType.Whisper => _chat.WrapWhisperMessage(ent,
+                "chat-manager-entity-whisper-wrap-message",
+                name,
+                args.Message),
+            InGameICChatType.Emote => Loc.GetString("chat-manager-entity-me-wrap-message",
+                ("entityName", Name(ent)),
+                ("entity", ent.Owner),
+                ("message", FormattedMessage.RemoveMarkupOrThrow(args.Message))),
+            _ => null,
+        };
+
+        if (wrapped == null)
+            return;
+
+        var chatChannel = channel switch
+        {
+            InGameICChatType.Whisper => ChatChannel.Whisper,
+            InGameICChatType.Emote => ChatChannel.Emotes,
+            _ => ChatChannel.Local,
+        };
+
+        if (_player.TryGetSessionByEntity(ent, out var ownSession))
+            _chatManager.ChatMessageToOne(chatChannel, args.Message, wrapped, ent, false, ownSession.Channel);
+
+        foreach (var session in _player.Sessions)
+        {
+            if (session.AttachedEntity is not { } attached
+                || attached == ent.Owner
+                || attached == ent.Comp.Listener
+                || !HasComp<GhostHearingComponent>(attached))
+                continue;
+
+            _chatManager.ChatMessageToOne(chatChannel, args.Message, wrapped, ent, false, session.Channel);
+        }
+
+        if (ent.Comp.Listener is not { } listener || TerminatingOrDeleted(listener))
+            return;
+
+        var range = channel == InGameICChatType.Whisper
+            ? SharedChatSystem.WhisperMuffledRange
+            : SharedChatSystem.VoiceRange;
+
+        if (Transform(ent).Coordinates.TryDistance(EntityManager, Transform(listener).Coordinates, out var distance)
+            && distance <= range
+            && _player.TryGetSessionByEntity(listener, out var listenerSession))
+            _chatManager.ChatMessageToOne(chatChannel, args.Message, wrapped, ent, false, listenerSession.Channel);
+    }
+}
diff --git a/Content.Goobstation.Server/Hallucinations/HallucinationSystem.cs b/Content.Goobstation.Server/Hallucinations/HallucinationSystem.cs
new file mode 100644
index 000000000000..d467b0476044
--- /dev/null
+++ b/Content.Goobstation.Server/Hallucinations/HallucinationSystem.cs
@@ -0,0 +1,351 @@
+using System.Diagnostics.CodeAnalysis;
+using System.Numerics;
+using Content.Goobstation.Shared.Hallucinations;
+using Content.Server.Administration.Logs;
+using Content.Server.Chat.Managers;
+using Content.Server.Chat.Systems;
+using Content.Shared.Database;
+using Content.Server.NPC.Systems;
+using Content.Server.Weapons.Melee;
+using Content.Shared.Chat;
+using Content.Shared.Eye;
+using Content.Shared.Humanoid;
+using Content.Shared.Mobs.Systems;
+using Content.Shared.NPC;
+using Content.Shared.Random;
+using Content.Shared.Random.Helpers;
+using Content.Shared.StatusEffectNew.Components;
+using Robust.Server.GameObjects;
+using Robust.Server.GameStates;
+using Robust.Server.Player;
+using Robust.Shared.Audio.Systems;
+using Robust.Shared.Map;
+using Robust.Shared.Player;
+using Robust.Shared.Prototypes;
+using Robust.Shared.Random;
+using Robust.Shared.Timing;
+
+namespace Content.Goobstation.Server.Hallucinations;
+
+/// <summary>
+/// Runs hallucinations.
+/// </summary>
+public sealed class HallucinationSystem : EntitySystem
+{
+    [Dependency] private readonly IAdminLogManager _adminLog = default!;
+    [Dependency] private readonly SharedAudioSystem _audio = default!;
+    [Dependency] private readonly ChatSystem _chat = default!;
+    [Dependency] private readonly IChatManager _chatManager = default!;
+    [Dependency] private readonly SharedEyeSystem _eye = default!;
+    [Dependency] private readonly EntityLookupSystem _lookup = default!;
+    [Dependency] private readonly MeleeWeaponSystem _melee = default!;
+    [Dependency] private readonly MobStateSystem _mobState = default!;
+    [Dependency] private readonly IPlayerManager _player = default!;
+    [Dependency] private readonly IPrototypeManager _proto = default!;
+    [Dependency] private readonly PvsOverrideSystem _pvsOverride = default!;
+    [Dependency] private readonly IRobustRandom _random = default!;
+    [Dependency] private readonly NPCSteeringSystem _steering = default!;
+    [Dependency] private readonly IGameTiming _timing = default!;
+    [Dependency] private readonly TransformSystem _transform = default!;
+    [Dependency] private readonly VisibilitySystem _visibility = default!;
+
+    public override void Initialize()
+    {
+        base.Initialize();
+
+        SubscribeLocalEvent<HallucinatingComponent, MapInitEvent>(OnStart);
+        SubscribeLocalEvent<HallucinatingComponent, ComponentShutdown>(OnShutdown);
+    }
+
+    private void OnStart(Entity<HallucinatingComponent> ent, ref MapInitEvent args)
+    {
+        _adminLog.Add(LogType.EntityEffect, LogImpact.Medium, $"{ToPrettyString(ent):entity} started hallucinating");
+    }
+
+    public override void Update(float frameTime)
+    {
+        base.Update(frameTime);
+
+        var now = _timing.CurTime;
+
+        var query = EntityQueryEnumerator<HallucinatingComponent>();
+        while (query.MoveNext(out var uid, out var comp))
+        {
+            var victim = TryComp<StatusEffectComponent>(uid, out var status) ? status.AppliedTo : uid;
+
+            UpdateVictim((uid, comp), victim);
+
+            if (victim is { } v)
+                UpdateHallucinating(v, comp, now);
+        }
+
+        var phantoms = EntityQueryEnumerator<PhantomHallucinationComponent>();
+        while (phantoms.MoveNext(out var uid, out var phantom))
+            UpdatePhantom(uid, phantom, now);
+    }
+
+    private void UpdateVictim(Entity<HallucinatingComponent> ent, EntityUid? victim)
+    {
+        if (ent.Comp.CurrentVictim == victim)
+            return;
+
+        if (ent.Comp.CurrentVictim is { } old)
+        {
+            SetHallucinationVisibility(old, false);
+            CleanupPhantoms(old);
+        }
+
+        if (victim is { } fresh)
+            SetHallucinationVisibility(fresh, true);
+
+        ent.Comp.CurrentVictim = victim;
+    }
+
+    private void OnShutdown(Entity<HallucinatingComponent> ent, ref ComponentShutdown args)
+    {
+        if (ent.Comp.CurrentVictim is not { } victim)
+            return;
+
+        SetHallucinationVisibility(victim, false);
+        CleanupPhantoms(victim);
+    }
+
+    private void SetHallucinationVisibility(EntityUid victim, bool enabled)
+    {
+        if (!TryComp<EyeComponent>(victim, out var eye))
+            return;
+
+        var mask = enabled
+            ? eye.VisibilityMask | (int) VisibilityFlags.Hallucination
+            : eye.VisibilityMask & ~(int) VisibilityFlags.Hallucination;
+        _eye.SetVisibilityMask(victim, mask, eye);
+    }
+
+    private void CleanupPhantoms(EntityUid victim)
+    {
+        var query = EntityQueryEnumerator<PhantomHallucinationComponent>();
+        while (query.MoveNext(out var mob, out var phantom))
+        {
+            if (phantom.Victim == victim)
+                QueueDel(mob);
+        }
+    }
+
+    #region Timer
+
+    private void UpdateHallucinating(EntityUid victim, HallucinatingComponent comp, TimeSpan now)
+    {
+        if (comp.NextTime == TimeSpan.Zero)
+        {
+            comp.NextTime = now + NextDelay(comp);
+            return;
+        }
+
+        if (now < comp.NextTime)
+            return;
+
+        comp.NextTime = now + NextDelay(comp);
+
+        if (TryPick(comp, out var proto))
+            Fire(victim, proto.Effect);
+    }
+
+    private TimeSpan NextDelay(HallucinatingComponent comp)
+    {
+        var severity = MathF.Max(0.01f, comp.Severity);
+        return TimeSpan.FromSeconds(_random.NextFloat(comp.DelayMin, comp.DelayMax) / severity);
+    }
+
+    /// <summary>
+    /// Picks a hallucination group from the table, then a hallucination from within that group.
+    /// </summary>
+    private bool TryPick(HallucinatingComponent comp, [NotNullWhen(true)] out HallucinationPrototype? picked)
+    {
+        picked = null;
+
+        if (!_proto.TryIndex(comp.Groups, out var groups) || groups.Weights.Count == 0)
+            return false;
+
+        if (!_proto.TryIndex<WeightedRandomPrototype>(groups.Pick(_random), out var group) || group.Weights.Count == 0)
+            return false;
+
+        return _proto.TryIndex(group.Pick(_random), out picked);
+    }
+
+    #endregion
+
+    #region Effects
+
+    private void Fire(EntityUid victim, HallucinationEffect effect)
+    {
+        switch (effect)
+        {
+            case MobHallucinationEffect mob:
+                SpawnPhantoms(victim, mob);
+                break;
+            case SoundHallucinationEffect sound:
+                PlayPhantomSound(victim, sound);
+                break;
+            case AppearanceHallucinationEffect appearance:
+                SendAppearance(victim, appearance);
+                break;
+            case SpeechHallucinationEffect speech:
+                PlayFakeSpeech(victim, speech);
+                break;
+            case MorphHallucinationEffect morph:
+                SendMorph(victim, morph);
+                break;
+        }
+    }
+
+    /// <summary>
+    /// Tells the victims client to make a nearby bystander look like one of the mobs.
+    /// </summary>
+    private void SendMorph(EntityUid victim, MorphHallucinationEffect effect)
+    {
+        if (effect.Mobs.Count == 0 || !_player.TryGetSessionByEntity(victim, out var session))
+            return;
+
+        var mobs = new List<string>();
+        foreach (var mob in effect.Mobs)
+            mobs.Add(mob);
+
+        RaiseNetworkEvent(new SetHallucinationMorphMessage(mobs), session);
+    }
+
+    /// <summary>
+    /// Fake conversation: a nearby bystander (or, in mass mode, everyone nearby, repeatedly) seems
+    /// to speak to the victim. The chat and speech bubble go to the victims client alone.
+    /// </summary>
+    private void PlayFakeSpeech(EntityUid victim, SpeechHallucinationEffect effect)
+    {
+        if (!_proto.TryIndex(effect.Messages, out var dataset)
+            || dataset.Values.Count == 0
+            || !_player.TryGetSessionByEntity(victim, out var session))
+            return;
+
+        var speakers = new List<EntityUid>();
+        foreach (var ent in _lookup.GetEntitiesInRange<HumanoidAppearanceComponent>(Transform(victim).Coordinates, effect.Range))
+            if (ent.Owner != victim)
+                speakers.Add(ent.Owner);
+
+        if (speakers.Count == 0)
+            return;
+
+        var line = dataset.Values[_random.Next(dataset.Values.Count)];
+        var message = Loc.GetString(line, ("name", Name(victim)));
+
+        if (!effect.Mass)
+        {
+            FakeSay(_random.Pick(speakers), message, session);
+            return;
+        }
+
+        foreach (var speaker in speakers)
+        {
+            var repeats = _random.Next(effect.RepeatMin, effect.RepeatMax + 1);
+            for (var i = 0; i < repeats; i++)
+                FakeSay(speaker, message, session);
+        }
+    }
+
+    private void FakeSay(EntityUid speaker, string message, ICommonSession session)
+    {
+        var wrapped = _chat.WrapPublicMessage(speaker, Name(speaker), message);
+        _chatManager.ChatMessageToOne(ChatChannel.Local, message, wrapped, speaker, false, session.Channel);
+    }
+
+    private void SpawnPhantoms(EntityUid victim, MobHallucinationEffect effect)
+    {
+        if (effect.Mobs.Count == 0)
+            return;
+
+        var xform = Transform(victim);
+        if (xform.GridUid == null)
+            return;
+
+        var count = _random.Next(effect.CountMin, effect.CountMax + 1);
+        for (var i = 0; i < count; i++)
+        {
+            var distance = _random.NextFloat(effect.RangeMin, effect.RangeMax);
+            var coords = xform.Coordinates.Offset(_random.NextAngle().ToVec() * distance);
+
+            var mob = Spawn(_random.Pick(effect.Mobs), coords);
+            MakeHallucination(mob, victim);
+
+            var phantom = EnsureComp<PhantomHallucinationComponent>(mob);
+            phantom.Victim = victim;
+            phantom.AttackSound = effect.AttackSound;
+            phantom.AttackRange = effect.AttackRange;
+            phantom.AttackDelayMin = effect.AttackDelayMin;
+            phantom.AttackDelayMax = effect.AttackDelayMax;
+
+            EnsureComp<ActiveNPCComponent>(mob);
+            _steering.Register(mob, new EntityCoordinates(victim, Vector2.Zero));
+        }
+    }
+
+    private void PlayPhantomSound(EntityUid victim, SoundHallucinationEffect effect)
+    {
+        if (!_player.TryGetSessionByEntity(victim, out var session))
+            return;
+
+        var offset = _random.NextAngle().ToVec() * _random.NextFloat(effect.RangeMin, effect.RangeMax);
+        var coords = Transform(victim).Coordinates.Offset(offset);
+
+        _audio.PlayStatic(effect.Sound, Filter.SinglePlayer(session), coords, false);
+    }
+
+    private void SendAppearance(EntityUid victim, AppearanceHallucinationEffect effect)
+    {
+        if (effect.Prototypes.Count == 0 || effect.States.Count == 0
+            || !_player.TryGetSessionByEntity(victim, out var session))
+            return;
+
+        RaiseNetworkEvent(new SetHallucinationAppearanceMessage(effect.Prototypes, effect.States, effect.Sound), session);
+    }
+
+    #endregion
+
+    #region Phantoms
+
+    private void UpdatePhantom(EntityUid uid, PhantomHallucinationComponent phantom, TimeSpan now)
+    {
+        var victim = phantom.Victim;
+        if (TerminatingOrDeleted(victim) || _mobState.IsDead(victim))
+            return;
+
+        _steering.Register(uid, new EntityCoordinates(victim, Vector2.Zero));
+
+        if (now < phantom.NextAttack
+            || !Transform(uid).Coordinates.TryDistance(EntityManager, Transform(victim).Coordinates, out var distance)
+            || distance > phantom.AttackRange)
+            return;
+
+        phantom.NextAttack = now + TimeSpan.FromSeconds(_random.NextFloat(phantom.AttackDelayMin, phantom.AttackDelayMax));
+        Swing(uid, victim, phantom);
+    }
+
+    private void Swing(EntityUid uid, EntityUid victim, PhantomHallucinationComponent phantom)
+    {
+        var diff = _transform.GetWorldPosition(victim) - _transform.GetWorldPosition(uid);
+        if (diff.LengthSquared() > 0f)
+            _melee.DoLunge(uid, uid, new Angle(diff), Vector2.Normalize(diff) * 0.5f, null, Angle.Zero, false, predicted: false);
+
+        if (phantom.AttackSound != null && _player.TryGetSessionByEntity(victim, out var session))
+            _audio.PlayEntity(phantom.AttackSound, session, uid);
+    }
+
+    #endregion
+
+    /// <summary>
+    /// Hides an entity on the hallucination visibility layer and force-sends it to the victim.
+    /// </summary>
+    public void MakeHallucination(EntityUid ent, EntityUid victim)
+    {
+        _visibility.SetLayer(ent, (ushort) VisibilityFlags.Hallucination);
+
+        if (_player.TryGetSessionByEntity(victim, out var session))
+            _pvsOverride.AddForceSend(ent, session);
+    }
+}
diff --git a/Content.Goobstation.Shared/Hallucinations/HallucinatingComponent.cs b/Content.Goobstation.Shared/Hallucinations/HallucinatingComponent.cs
new file mode 100644
index 000000000000..34f4dd9b9d32
--- /dev/null
+++ b/Content.Goobstation.Shared/Hallucinations/HallucinatingComponent.cs
@@ -0,0 +1,40 @@
+using Content.Shared.Random;
+using Robust.Shared.Prototypes;
+
+namespace Content.Goobstation.Shared.Hallucinations;
+
+/// <summary>
+/// While this is present the entity starts hallucinating. Hallucinations include fake mobs, fake clothes / items on players, and fake sounds.
+/// </summary>
+[RegisterComponent]
+public sealed partial class HallucinatingComponent : Component
+{
+    /// <summary>
+    /// Weighted table of hallucination groups.
+    /// </summary>
+    [DataField]
+    public ProtoId<WeightedRandomPrototype> Groups = "HallucinationGroups";
+
+    /// <summary>
+    /// Scales how often hallucinations fire. the delay between them is divided by this,
+    /// so 2 means twice as often (15-60s at base delays), 0.5 means half as often (60-240s).
+    /// At the default 1 the base 30-120s range applies.
+    /// </summary>
+    [DataField]
+    public float Severity = 1f;
+
+    /// <summary>
+    /// Base delay between hallucinations.
+    /// </summary>
+    [DataField]
+    public float DelayMin = 30f;
+
+    [DataField]
+    public float DelayMax = 120f;
+
+    [ViewVariables]
+    public TimeSpan NextTime;
+
+    [DataField]
+    public EntityUid? CurrentVictim;
+}
diff --git a/Content.Goobstation.Shared/Hallucinations/HallucinationEffects.cs b/Content.Goobstation.Shared/Hallucinations/HallucinationEffects.cs
new file mode 100644
index 000000000000..52b882412b68
--- /dev/null
+++ b/Content.Goobstation.Shared/Hallucinations/HallucinationEffects.cs
@@ -0,0 +1,135 @@
+using Content.Shared.Dataset;
+using Robust.Shared.Audio;
+using Robust.Shared.Prototypes;
+using Robust.Shared.Serialization;
+
+namespace Content.Goobstation.Shared.Hallucinations;
+
+/// <summary>
+/// Spawns phantom mobs near the victim.
+/// </summary>
+public sealed partial class MobHallucinationEffect : HallucinationEffect
+{
+    [DataField(required: true)]
+    public List<EntProtoId> Mobs = new();
+
+    [DataField]
+    public int CountMin = 1;
+
+    [DataField]
+    public int CountMax = 2;
+
+    /// <summary>
+    /// How far from the victim a phantom spawns.
+    /// </summary>
+    [DataField]
+    public float RangeMin = 3f;
+
+    [DataField]
+    public float RangeMax = 6f;
+
+    /// <summary>
+    /// How close a phantom must be to swing at the victim.
+    /// </summary>
+    [DataField]
+    public float AttackRange = 1.5f;
+
+    [DataField]
+    public float AttackDelayMin = 1.5f;
+
+    [DataField]
+    public float AttackDelayMax = 3f;
+
+    [DataField]
+    public SoundSpecifier? AttackSound = new SoundCollectionSpecifier("Punch");
+}
+
+/// <summary>
+/// Plays a sound from a random point near the victim.
+/// </summary>
+public sealed partial class SoundHallucinationEffect : HallucinationEffect
+{
+    [DataField(required: true)]
+    public SoundSpecifier Sound = default!;
+
+    [DataField]
+    public float RangeMin = 2f;
+
+    [DataField]
+    public float RangeMax = 7f;
+}
+
+/// <summary>
+/// Fake conversations.
+/// </summary>
+public sealed partial class SpeechHallucinationEffect : HallucinationEffect
+{
+    /// <summary>
+    /// Dataset of lines a hallucinated voice can say.
+    /// </summary>
+    [DataField]
+    public ProtoId<LocalizedDatasetPrototype> Messages = "HallucinationSpeech";
+
+    /// <summary>
+    /// If true, everyone nearby chants the same line several times each instead of one person saying it once.
+    /// </summary>
+    [DataField]
+    public bool Mass;
+
+    [DataField]
+    public float Range = 6f;
+
+    /// <summary>
+    /// In mass mode, how many times each nearby voice repeats the line.
+    /// </summary>
+    [DataField]
+    public int RepeatMin = 3;
+
+    [DataField]
+    public int RepeatMax = 9;
+}
+
+/// <summary>
+/// Adds a fake held item / worn clothing sprite layer to a random nearby bystander for a few seconds.
+/// </summary>
+public sealed partial class AppearanceHallucinationEffect : HallucinationEffect
+{
+    [DataField(required: true)]
+    public List<string> Prototypes = new();
+
+    [DataField(required: true)]
+    public List<string> States = new();
+
+    [DataField]
+    public SoundSpecifier? Sound;
+}
+
+/// <summary>
+/// Network message for AppearanceHallucinationEffect.
+/// </summary>
+[Serializable, NetSerializable]
+public sealed class SetHallucinationAppearanceMessage(List<string> prototypes, List<string> states, SoundSpecifier? sound)
+    : EntityEventArgs
+{
+    public List<string> Prototypes = prototypes;
+    public List<string> States = states;
+    public SoundSpecifier? Sound = sound;
+}
+
+/// <summary>
+/// Fully replaces a random nearby bystander's sprite with one of these mobs for a few seconds.
+/// </summary>
+public sealed partial class MorphHallucinationEffect : HallucinationEffect
+{
+    [DataField(required: true)]
+    public List<EntProtoId> Mobs = new();
+}
+
+/// <summary>
+/// Network message for MorphHallucinationEffect.
+/// </summary>
+[Serializable, NetSerializable]
+public sealed class SetHallucinationMorphMessage(List<string> mobs) : EntityEventArgs
+{
+    public List<string> Mobs = mobs;
+}
diff --git a/Content.Goobstation.Shared/Hallucinations/HallucinationPrototype.cs b/Content.Goobstation.Shared/Hallucinations/HallucinationPrototype.cs
new file mode 100644
index 000000000000..19994331e8f5
--- /dev/null
+++ b/Content.Goobstation.Shared/Hallucinations/HallucinationPrototype.cs
@@ -0,0 +1,19 @@
+using Robust.Shared.Prototypes;
+
+namespace Content.Goobstation.Shared.Hallucinations;
+
+/// <summary>
+/// Hallucinations used by the hallucination system.
+/// </summary>
+[Prototype]
+public sealed partial class HallucinationPrototype : IPrototype
+{
+    [IdDataField]
+    public string ID { get; private set; } = default!;
+
+    [DataField(required: true)]
+    public HallucinationEffect Effect = default!;
+}
+
+[ImplicitDataDefinitionForInheritors]
+public abstract partial class HallucinationEffect;
diff --git a/Content.Goobstation.Shared/Hallucinations/PhantomHallucinationComponent.cs b/Content.Goobstation.Shared/Hallucinations/PhantomHallucinationComponent.cs
new file mode 100644
index 000000000000..493c37ddc602
--- /dev/null
+++ b/Content.Goobstation.Shared/Hallucinations/PhantomHallucinationComponent.cs
@@ -0,0 +1,31 @@
+using Robust.Shared.Audio;
+
+namespace Content.Goobstation.Shared.Hallucinations;
+
+/// <summary>
+/// A phantom mob conjured by a hallucination.
+/// </summary>
+[RegisterComponent]
+public sealed partial class PhantomHallucinationComponent : Component
+{
+    [DataField]
+    public EntityUid Victim;
+
+    [DataField]
+    public SoundSpecifier? AttackSound;
+
+    /// <summary>
+    /// How close the phantom must be to swing.
+    /// </summary>
+    [DataField]
+    public float AttackRange = 1.5f;
+
+    [DataField]
+    public float AttackDelayMin = 1.5f;
+
+    [DataField]
+    public float AttackDelayMax = 3f;
+
+    [DataField]
+    public TimeSpan NextAttack;
+}
diff --git a/Content.Server/Chat/Systems/ChatSystem.cs b/Content.Server/Chat/Systems/ChatSystem.cs
index e1e08e588420..9ff11966f276 100644
--- a/Content.Server/Chat/Systems/ChatSystem.cs
+++ b/Content.Server/Chat/Systems/ChatSystem.cs
@@ -264,6 +264,13 @@ public override void TrySendInGameICMessage(
 
         message = SanitizeInGameICMessage(source, message, out var emoteStr, shouldCapitalize, shouldPunctuate, shouldCapitalizeTheWordI);
 
+        // Goobstation - hallucinations start
+        var beforeEv = new BeforeChatMessageSentEvent(message, (byte) desiredType);
+        RaiseLocalEvent(source, ref beforeEv);
+        if (beforeEv.Cancelled)
+            return;
+        // Goobstation end
+
         // Was there an emote in the message? If so, send it.
         if (player != null && emoteStr != message && emoteStr != null)
         {
diff --git a/Content.Server/Ghost/GhostSystem.cs b/Content.Server/Ghost/GhostSystem.cs
index 8b437063a6ca..b50da2aacd78 100644
--- a/Content.Server/Ghost/GhostSystem.cs
+++ b/Content.Server/Ghost/GhostSystem.cs
@@ -133,6 +133,7 @@ private void OnGhostVis(Entity<GhostComponent> ent, ref GetVisMaskEvent args)
                 // Begin DeltaV additions
                 args.VisibilityMask |= (int)VisibilityFlags.CosmicCultMonument;
                 // End DeltaV additions
+                args.VisibilityMask |= (int)VisibilityFlags.Hallucination; // Goobstation
             }
         }
 
diff --git a/Content.Shared/Eye/VisibilityFlags.cs b/Content.Shared/Eye/VisibilityFlags.cs
index 97dd6f635aed..f4d901207955 100644
--- a/Content.Shared/Eye/VisibilityFlags.cs
+++ b/Content.Shared/Eye/VisibilityFlags.cs
@@ -19,5 +19,6 @@ public enum VisibilityFlags : int
         EldritchInfluence = 1 << 5, // Goobstation
         EldritchInfluenceSpent = 1 << 6, // Goobstation
         Admin = 1 << 7, // Reserved for admins in stealth mode and admin tools.
+        Hallucination = 1 << 10, // Goobstation - hallucinations, visible only to the hallucinating player.
     }
 }
diff --git a/Resources/Locale/en-US/_Goobstation/hallucinations/fake-conversations.ftl b/Resources/Locale/en-US/_Goobstation/hallucinations/fake-conversations.ftl
new file mode 100644
index 000000000000..45fa57b1d119
--- /dev/null
+++ b/Resources/Locale/en-US/_Goobstation/hallucinations/fake-conversations.ftl
@@ -0,0 +1,60 @@
+hallucination-speech-1 = Hey {$name}, you got a second?
+hallucination-speech-2 = {$name}, c'mere for a sec.
+hallucination-speech-3 = {$name}, come look at this.
+hallucination-speech-4 = Hey {$name}, you dropped something.
+hallucination-speech-5 = {$name}, can you hold this for me?
+hallucination-speech-6 = {$name}, they need you in the back.
+hallucination-speech-7 = {$name}? I've been looking all over for you.
+hallucination-speech-8 = Where've you been, {$name}? It's been hours.
+hallucination-speech-9 = {$name}, someone was asking about you.
+hallucination-speech-10 = {$name}, we really need to talk.
+hallucination-speech-11 = {$name}... did you hear that?
+hallucination-speech-12 = You feeling alright, {$name}? You look pale.
+hallucination-speech-13 = You've been acting weird all shift, {$name}.
+hallucination-speech-14 = You okay, {$name}? You're shaking.
+hallucination-speech-15 = {$name}, your hands are bleeding.
+hallucination-speech-16 = {$name}, when did you last sleep?
+hallucination-speech-17 = Why do you keep staring at me, {$name}?
+hallucination-speech-18 = Who are you talking to, {$name}?
+hallucination-speech-19 = {$name}, there's nobody there.
+hallucination-speech-20 = {$name}, we can all hear you.
+hallucination-speech-21 = {$name}, you're scaring people.
+hallucination-speech-22 = {$name}, you keep saying that. Stop it.
+hallucination-speech-23 = Everyone's been talking about you, {$name}.
+hallucination-speech-24 = Something's wrong with you, {$name}. I can tell.
+hallucination-speech-25 = Maybe you should sit down, {$name}, before you hurt someone.
+hallucination-speech-26 = {$name}, put it down. Please.
+hallucination-speech-27 = I saw what you did, {$name}.
+hallucination-speech-28 = {$name}, they know. They all know.
+hallucination-speech-29 = {$name}, it's your fault. All of it.
+hallucination-speech-30 = {$name}, you brought this on yourself.
+hallucination-speech-31 = {$name}, you can't trust any of them.
+hallucination-speech-32 = {$name}, nobody's coming to help you.
+hallucination-speech-33 = {$name}, what's that behind you?
+hallucination-speech-34 = Don't turn around, {$name}.
+hallucination-speech-35 = {$name}, it's right next to you.
+hallucination-speech-36 = They're coming for you, {$name}. All of them.
+hallucination-speech-37 = I can see it in your eyes, {$name}.
+hallucination-speech-38 = You're one of them now, aren't you, {$name}?
+hallucination-speech-39 = You're not real, are you, {$name}?
+hallucination-speech-40 = {$name}, I'd run if I were you.
+hallucination-speech-41 = {$name}, get out while you still can.
+hallucination-speech-42 = Quit following me around, {$name}.
+hallucination-speech-43 = Stay back, {$name}.
+hallucination-speech-44 = Don't touch me, {$name}.
+hallucination-speech-45 = Get away from me, {$name}.
+hallucination-speech-46 = {$name}, we're going to have to put you down.
+hallucination-speech-47 = {$name}, you're already dead. You just don't know it yet.
+hallucination-speech-48 = {$name}, hold still. This won't hurt.
+hallucination-speech-49 = {$name}, I've got something for you.
+hallucination-speech-50 = {$name}, look what you made me do.
+hallucination-speech-51 = {$name}, I've been waiting for this.
+hallucination-speech-52 = {$name}, I'm right behind you.
+hallucination-speech-53 = {$name}, there's nowhere to hide.
+hallucination-speech-54 = This is going to hurt, {$name}.
+hallucination-speech-55 = Scream all you want, {$name}.
+hallucination-speech-56 = {$name}, I'm gonna make you hurt.
+hallucination-speech-57 = {$name}, I'm going to enjoy this.
+hallucination-speech-58 = {$name}, say goodbye.
+hallucination-speech-59 = I'm going to hurt you, {$name}.
+hallucination-speech-60 = I'm going to kill you, {$name}.
diff --git a/Resources/Prototypes/Reagents/toxins.yml b/Resources/Prototypes/Reagents/toxins.yml
index 01f8f9076ce4..90615ab9d0df 100644
--- a/Resources/Prototypes/Reagents/toxins.yml
+++ b/Resources/Prototypes/Reagents/toxins.yml
@@ -397,7 +397,10 @@
         effectProto: StatusEffectSeeingRainbow
         type: Add
         time: 10
-  # TODO: PROPER hallucinations
+      - !type:ModifyStatusEffect # Goobstation - hallucinations
+        effectProto: StatusEffectHallucinating
+        type: Add
+        time: 20
 
 - type: reagent
   id: Histamine
diff --git a/Resources/Prototypes/_Goobstation/Hallucinations/hallucinations.yml b/Resources/Prototypes/_Goobstation/Hallucinations/hallucinations.yml
new file mode 100644
index 000000000000..05a2759d5b0d
--- /dev/null
+++ b/Resources/Prototypes/_Goobstation/Hallucinations/hallucinations.yml
@@ -0,0 +1,752 @@
+- type: weightedRandom
+  id: HallucinationGroups
+  weights:
+    HallucinationGroupSounds: 3
+    HallucinationGroupMobs: 2
+    HallucinationGroupMorphs: 2
+    HallucinationGroupAppearances: 2
+    HallucinationGroupSpeech: 1
+
+- type: weightedRandom
+  id: HallucinationGroupSounds
+  weights:
+    HallucinationWhispers: 4
+    HallucinationFootsteps: 3
+    HallucinationKnocking: 2
+    HallucinationScreams: 2
+    HallucinationScreamsFemale: 2
+    HallucinationZombieGroans: 1.5
+    HallucinationLaughter: 2
+    HallucinationGiggling: 2
+    HallucinationHissing: 1.5
+    HallucinationGrowling: 1.5
+    HallucinationGlassBreak: 2
+    HallucinationMetalBang: 2
+    HallucinationBodyfall: 1.5
+    HallucinationBonesRattle: 1.5
+    HallucinationDemonGrowl: 1
+    HallucinationExplosionFar: 1
+    HallucinationChurchBell: 1
+    HallucinationOminous: 1
+    HallucinationSparks: 1.5
+    HallucinationSpaceWind: 1
+    HallucinationAlarm: 1
+    HallucinationBuzzing: 1.5
+    HallucinationVentHiss: 1.5
+    HallucinationAirlockCreak: 1.5
+
+- type: weightedRandom
+  id: HallucinationGroupMobs
+  weights:
+    HallucinationSpiders: 1.5
+    HallucinationClownSpiders: 1
+    HallucinationVermin: 1.5
+    HallucinationBeasts: 1
+    HallucinationXenos: 0.8
+    HallucinationSlimes: 1
+    HallucinationHorrors: 0.5
+
+- type: weightedRandom
+  id: HallucinationGroupMorphs
+  weights:
+    HallucinationImpostor: 1.5
+    HallucinationImpostorHorror: 0.5
+
+- type: weightedRandom
+  id: HallucinationGroupAppearances
+  weights:
+    HallucinationChangeling: 2
+    HallucinationMansus: 1
+    HallucinationHeretic: 1.5
+    HallucinationGunman: 1.5
+    HallucinationKnife: 2.5
+    HallucinationMachete: 1.5
+    HallucinationKatana: 1
+    HallucinationEnergyBlade: 1
+    HallucinationCultDagger: 1
+    HallucinationFireaxe: 1
+    HallucinationRifle: 1.5
+    HallucinationShotgun: 1
+    HallucinationSmg: 1
+
+- type: weightedRandom
+  id: HallucinationGroupSpeech
+  weights:
+    HallucinationFakeSpeech: 3
+    HallucinationFakeSpeechMass: 0.5
+
+# Sounds
+
+- type: hallucination
+  id: HallucinationWhispers
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: Paracusia
+
+- type: hallucination
+  id: HallucinationFootsteps
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: FootstepFloor
+    rangeMin: 2
+    rangeMax: 5
+
+- type: hallucination
+  id: HallucinationKnocking
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/glass_knock.ogg
+
+- type: hallucination
+  id: HallucinationScreams
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: MaleScreams
+
+- type: hallucination
+  id: HallucinationScreamsFemale
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: FemaleScreams
+
+- type: hallucination
+  id: HallucinationZombieGroans
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: ZombieScreams
+
+- type: hallucination
+  id: HallucinationLaughter
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: MaleLaugh
+
+- type: hallucination
+  id: HallucinationGiggling
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: FemaleLaugh
+
+- type: hallucination
+  id: HallucinationHissing
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: HarpyHisses
+
+- type: hallucination
+  id: HallucinationGrowling
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: HarpyGrowls
+
+- type: hallucination
+  id: HallucinationGlassBreak
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      collection: GlassBreak
+
+- type: hallucination
+  id: HallucinationMetalBang
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/metal_slam1.ogg
+
+- type: hallucination
+  id: HallucinationBodyfall
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/bodyfall1.ogg
+
+- type: hallucination
+  id: HallucinationBonesRattle
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/bone_rattle.ogg
+
+- type: hallucination
+  id: HallucinationDemonGrowl
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/demon_attack1.ogg
+
+- type: hallucination
+  id: HallucinationExplosionFar
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/explosionfar.ogg
+    rangeMin: 4
+    rangeMax: 8
+
+- type: hallucination
+  id: HallucinationChurchBell
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/church_bell1.ogg
+
+- type: hallucination
+  id: HallucinationOminous
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/ominous.ogg
+
+- type: hallucination
+  id: HallucinationSparks
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/sparks1.ogg
+
+- type: hallucination
+  id: HallucinationSpaceWind
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Effects/space_wind.ogg
+
+- type: hallucination
+  id: HallucinationAlarm
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Ambience/Objects/alarm.ogg
+
+- type: hallucination
+  id: HallucinationBuzzing
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Ambience/Objects/buzzing.ogg
+
+- type: hallucination
+  id: HallucinationVentHiss
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Ambience/Objects/gas_hiss.ogg
+
+- type: hallucination
+  id: HallucinationAirlockCreak
+  effect:
+    !type:SoundHallucinationEffect
+    sound:
+      path: /Audio/Machines/airlock_creaking.ogg
+
+# Mobs
+
+- type: hallucination
+  id: HallucinationSpiders
+  effect:
+    !type:MobHallucinationEffect
+    mobs:
+    - MobHallucinationSpider
+    countMin: 1
+    countMax: 2
+
+- type: hallucination
+  id: HallucinationClownSpiders
+  effect:
+    !type:MobHallucinationEffect
+    mobs:
+    - MobHallucinationClownSpider
+    countMin: 1
+    countMax: 3
+
+- type: hallucination
+  id: HallucinationVermin
+  effect:
+    !type:MobHallucinationEffect
+    mobs:
+    - MobHallucinationRat
+    - MobHallucinationCockroach
+    - MobHallucinationBat
+    - MobHallucinationSnake
+    countMin: 1
+    countMax: 3
+
+- type: hallucination
+  id: HallucinationBeasts
+  effect:
+    !type:MobHallucinationEffect
+    mobs:
+    - MobHallucinationBear
+    - MobHallucinationCarp
+    - MobHallucinationSharkminnow
+    countMin: 1
+    countMax: 2
+
+- type: hallucination
+  id: HallucinationXenos
+  effect:
+    !type:MobHallucinationEffect
+    mobs:
+    - MobHallucinationXeno
+    - MobHallucinationRunner
+    countMin: 1
+    countMax: 2
+
+- type: hallucination
+  id: HallucinationSlimes
+  effect:
+    !type:MobHallucinationEffect
+    mobs:
+    - MobHallucinationSlime
+    countMin: 1
+    countMax: 2
+
+- type: hallucination
+  id: HallucinationHorrors
+  effect:
+    !type:MobHallucinationEffect
+    mobs:
+    - MobHallucinationGoliath
+    - MobHallucinationHellspawn
+    - MobHallucinationFlesh
+    countMin: 1
+    countMax: 1
+
+# A person looks like one of the mobs (only the victim sees it)
+
+- type: hallucination
+  id: HallucinationImpostor
+  effect:
+    !type:MorphHallucinationEffect
+    mobs:
+    - MobHallucinationSpider
+    - MobHallucinationClownSpider
+    - MobHallucinationRat
+    - MobHallucinationSnake
+    - MobHallucinationBear
+    - MobHallucinationCarp
+    - MobHallucinationSharkminnow
+    - MobHallucinationCockroach
+    - MobHallucinationBat
+    - MobHallucinationSlime
+
+- type: hallucination
+  id: HallucinationImpostorHorror
+  effect:
+    !type:MorphHallucinationEffect
+    mobs:
+    - MobHallucinationXeno
+    - MobHallucinationRunner
+    - MobHallucinationGoliath
+    - MobHallucinationHellspawn
+    - MobHallucinationFlesh
+
+# Appearances (someone holding an l6, clothes, etc.)
+
+- type: hallucination
+  id: HallucinationChangeling
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - ArmBladeChangeling
+    - ArmClawChangeling
+    states:
+    - inhand-left
+    - inhand-right
+    sound:
+      collection: gib
+
+- type: hallucination
+  id: HallucinationMansus
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - TouchSpellMansus
+    states:
+    - inhand-left
+    - inhand-right
+    sound:
+      path: /Audio/Items/welder.ogg
+
+- type: hallucination
+  id: HallucinationHeretic
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - ClothingOuterArmorHeretic
+    states:
+    - equipped-OUTERCLOTHING
+    sound:
+      path: /Audio/Items/welder.ogg
+
+- type: hallucination
+  id: HallucinationGunman
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - WeaponLightMachineGunL6C
+    states:
+    - wielded-inhand-left
+    - wielded-inhand-right
+    sound:
+      path: /Audio/Weapons/Guns/Empty/lmg_empty.ogg
+
+- type: hallucination
+  id: HallucinationKnife
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - CombatKnife
+    - SurvivalKnife
+    - KitchenKnife
+    - PlasmaShiv
+    states:
+    - inhand-left
+    - inhand-right
+    sound:
+      path: /Audio/Weapons/bladeslice.ogg
+
+- type: hallucination
+  id: HallucinationMachete
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - Machete
+    - Cutlass
+    - CaptainSabre
+    states:
+    - inhand-left
+    - inhand-right
+    sound:
+      path: /Audio/Weapons/bladeslice.ogg
+
+- type: hallucination
+  id: HallucinationKatana
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - Katana
+    - EnergyKatana
+    states:
+    - inhand-left
+    - inhand-right
+    sound:
+      path: /Audio/Weapons/bladeslice.ogg
+
+- type: hallucination
+  id: HallucinationEnergyBlade
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - EnergyDagger
+    - EnergyCutlass
+    states:
+    - inhand-left
+    - inhand-right
+    sound:
+      path: /Audio/Weapons/ebladeon.ogg
+
+- type: hallucination
+  id: HallucinationCultDagger
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - RitualDagger
+    states:
+    - inhand-left
+    - inhand-right
+    sound:
+      collection: gib
+
+- type: hallucination
+  id: HallucinationFireaxe
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - FireAxe
+    states:
+    - wielded-inhand-left
+    - wielded-inhand-right
+
+- type: hallucination
+  id: HallucinationRifle
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - WeaponRifleAk
+    - WeaponRifleLecter
+    states:
+    - wielded-inhand-left
+    - wielded-inhand-right
+
+- type: hallucination
+  id: HallucinationShotgun
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - WeaponShotgunBulldog
+    states:
+    - wielded-inhand-left
+    - wielded-inhand-right
+
+- type: hallucination
+  id: HallucinationSmg
+  effect:
+    !type:AppearanceHallucinationEffect
+    prototypes:
+    - WeaponSubMachineGunC20r
+    states:
+    - wielded-inhand-left
+    - wielded-inhand-right
+
+# Fake conversations
+
+- type: hallucination
+  id: HallucinationFakeSpeech
+  effect:
+    !type:SpeechHallucinationEffect
+
+- type: hallucination
+  id: HallucinationFakeSpeechMass
+  effect:
+    !type:SpeechHallucinationEffect
+    mass: true
+
+- type: localizedDataset
+  id: HallucinationSpeech
+  values:
+    prefix: hallucination-speech-
+    count: 60
+
+# Status effect
+
+- type: entity
+  parent: MobStatusEffectDebuff
+  id: StatusEffectHallucinating
+  name: hallucinating
+  categories: [ HideSpawnMenu ]
+  components:
+  - type: Hallucinating
+
+# Base
+
+- type: entity
+  id: BaseMobHallucination
+  abstract: true
+  components:
+  - type: Sprite
+    noRot: true
+    drawdepth: Mobs
+  - type: Clickable
+  - type: Physics
+    bodyType: KinematicController
+  - type: Fixtures
+    fixtures:
+      fix1:
+        shape:
+          !type:PhysShapeCircle
+          radius: 0.40
+        density: 100
+        mask:
+        - SmallMobMask
+        layer:
+        - SmallMobLayer
+  - type: MovementAlwaysTouching
+  - type: MovementSpeedModifier
+    baseWalkSpeed: 3.5
+    baseSprintSpeed: 4.5
+  - type: StandingState
+  - type: Appearance
+  - type: InputMover
+  - type: MobMover
+  - type: NpcFactionMember
+    factions:
+    - SimpleNeutral
+  - type: TimedDespawn
+    lifetime: 10
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationSpider
+  name: tarantula
+  suffix: Hallucination
+  description: Widely recognized to be the literal worst thing in existence.
+  components:
+  - type: Sprite
+    layers:
+    - map: ["enum.DamageStateVisualLayers.Base", "movement"]
+      state: tarantula
+      sprite: Mobs/Animals/spider.rsi
+  - type: SpriteMovement
+    movementLayers:
+      movement:
+        state: tarantula-moving
+    noMovementLayers:
+      movement:
+        state: tarantula
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationClownSpider
+  name: clown spider
+  suffix: Hallucination
+  description: Combines the two most terrifying things in existence, spiders and clowns.
+  components:
+  - type: Sprite
+    layers:
+    - map: ["enum.DamageStateVisualLayers.Base"]
+      state: clown
+      sprite: Mobs/Animals/clownspider.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationRat
+  name: rat
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: rat
+      sprite: Mobs/Animals/rat.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationSnake
+  name: snake
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: snake
+      sprite: Mobs/Animals/snake.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationBear
+  name: bear
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: bear
+      sprite: Mobs/Animals/bear.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationCockroach
+  name: cockroach
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: cockroach
+      sprite: Mobs/Animals/cockroach.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationBat
+  name: bat
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: bat
+      sprite: Mobs/Animals/bat.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationCarp
+  name: space carp
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: alive
+      sprite: Mobs/Aliens/Carps/space.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationSharkminnow
+  name: sharkminnow
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: alive
+      sprite: Mobs/Aliens/Carps/sharkminnow.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationXeno
+  name: alien
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: running
+      sprite: Mobs/Aliens/Xenos/rouny.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationRunner
+  name: alien
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: running
+      sprite: Mobs/Aliens/Xenos/runner.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationGoliath
+  name: goliath
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: goliath
+      sprite: Mobs/Aliens/Asteroid/goliath.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationSlime
+  name: slime
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: blue_adult_slime
+      sprite: Mobs/Aliens/slimes.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationHellspawn
+  name: hellspawn
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: alive
+      sprite: Mobs/Demons/hellspawn.rsi
+
+- type: entity
+  parent: BaseMobHallucination
+  id: MobHallucinationFlesh
+  name: abomination
+  suffix: Hallucination
+  components:
+  - type: Sprite
+    layers:
+    - state: golem
+      sprite: Mobs/Aliens/flesh.rsi
